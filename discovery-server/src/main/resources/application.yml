spring:
  application:
    name: discovery-server
server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

---
# Docker profile: point clients at the compose service names
spring:
  config:
    activate:
      on-profile: docker
  application:
    name: discovery-server
server:
  port: 8761
