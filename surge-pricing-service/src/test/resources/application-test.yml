spring:
  application:
    name: surge-pricing-service
  redis:
    host: localhost
    port: 6379

server:
  port: 0

eureka:
  client:
    enabled: false

feign:
  client:
    config:
      default:
        connectTimeout: 1000
        readTimeout: 1000
